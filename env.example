# Email Configuration - SMTP Settings (Neo.space or any email provider)
SMTP_HOST=smtp.neo.space
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@neo.space
SMTP_PASSWORD=your-neo-space-password
SMTP_FROM=admin@atarwebb.com

# Common SMTP Providers:
# Neo.space: smtp.neo.space (port 587 or 465)
# Gmail: smtp.gmail.com (port 587)
# Outlook: smtp-mail.outlook.com (port 587)
# Yahoo: smtp.mail.yahoo.com (port 587)

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=WebApp Solutions

# Vercel Cron Job Security (Generate a random secret)
# Generate with: openssl rand -base64 32
CRON_SECRET=your-random-secret-here
